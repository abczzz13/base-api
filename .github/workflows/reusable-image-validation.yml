name: Reusable Image Validation

on:
  workflow_call:
    inputs:
      artifact_name:
        description: Uploaded artifact bundle name.
        required: false
        type: string
        default: image-validation-logs
      retention_days:
        description: Artifact retention period in days.
        required: false
        type: number
        default: 14

defaults:
  run:
    shell: bash

jobs:
  run:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Prepare / Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Verify / Image validation
        uses: ./.github/actions/run-image-validation
        with:
          artifact-name: ${{ inputs.artifact_name }}
          retention-days: ${{ inputs.retention_days }}
